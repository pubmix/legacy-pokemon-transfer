Synthetic test fixtures are generated by `tests/conftest.py`.

The repository intentionally does not store `.sav` files by default. This keeps
real cartridge or emulator saves from being published accidentally while still
allowing `pytest` to create deterministic synthetic fixtures locally.
